#include <stdio.h>
void mul(int n); 
int main() {
    int a;
    printf("Enter a number: "); 
    scanf("%d", &a);
    mul(a);
    return 0;
}

void mul(int n) {
    int i, m;
    for (i = 1; i <= 10; i++) {
        m = n * i;
        printf("%d x %d = %d\n", n, i, m); 
    }
}
