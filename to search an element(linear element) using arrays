#include <stdio.h>
int main() {
    int arr[5]={1,2,3,4,5}, key=3, found=0;
    for(int i=0;i<5;i++){
        if(arr[i]==key){
            found=1;
            break;
        }
    }
    if(found) printf("Found");
    else printf("Not Found");
    return 0;
}
