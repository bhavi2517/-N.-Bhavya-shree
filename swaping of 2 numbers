#include <stdio.h>
void main()
{
    int a = 10, b = 20, t;
    printf("Before swapping: a = %d\t b = %d\n", a, b);
    t = a;
    a = b;
    b = t;
  printf("After swapping: a = %d\t b = %d", a, b);
}
