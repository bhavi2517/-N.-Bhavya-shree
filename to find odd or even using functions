#include <stdio.h>
void num(int a);

int main() {
    int n;

    printf("Enter a number: ");
    scanf("%d", &n);
    num(n);

    return 0;
}
void num(int a) {
    if (a % 2 == 0) {
        printf("even\n");
    } else {
        printf("odd\n");
    }
}
