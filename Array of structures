#include <stdio.h>
struct Student {
    int id;
};

int main() {
    struct Student s[3];
    for(int i=0;i<3;i++)
        scanf("%d",&s[i].id);

    for(int i=0;i<3;i++)
        printf("%d ",s[i].id);

    return 0;
}
