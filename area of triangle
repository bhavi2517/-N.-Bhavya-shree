#include <stdio.h>
int main()
{
    float base, height, area;

    printf("Enter base of triangle: base,height");
    scanf("%f %f", &base,&height)
    area = (base * height) / 2.0;
    printf("Area of triangle = %.2f", area);
   return 0;
}
